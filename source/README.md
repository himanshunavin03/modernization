# Runtime Source Inputs

Customer and reference repositories placed under `source/` are local runtime inputs. They must not be committed to this customized solution repository and are always read-only to the scanner.

Provide any source project with `--source-root`; it does not need to live under this directory.
