# Semantic Providers

`tree_sitter` is the default batch syntax provider. `roslyn` is an optional C# semantic provider invoked by `--enable-roslyn`. `lsp` is reserved for future interactive definition/reference and IDE integration; no LSP server is implemented or required for batch analysis.
