# Prompt 032: Register Create Knowledge Graph Agent Command

Register `/create-knowledge-graph` as a thin, project-agnostic agent command over the existing deterministic CLI. Resolve source root and project ID safely, enable project-aware Roslyn only for .NET project evidence, keep Neo4j optional, invoke existing validation and artifact publishing, and never use an LLM to construct graph facts.
