# Prompt 083 — Associate commands with dashboard steps

Remove the standalone Polaris Commands section from the executive dashboard. Place every exact canonical command reference within the existing modernization lifecycle section it enables, so customers see each command in context with Architecture, Knowledge Graph and Understanding, Requirements, Angular modernization, Validation, or overall status. Preserve registry parity, responsive presentation quality, and the read-only `source/` boundary.
