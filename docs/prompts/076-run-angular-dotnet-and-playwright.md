# Prompt 076: Run Angular, .NET, and Playwright

## User request

> run angular, .net project and playwright

## Scope

- Reuse or start the verified legacy HealthClinic DNX/.NET backend on port 5000.
- Reuse or start the modern Angular application on port 4200.
- Verify both services and the Angular API proxy.
- Run the configured Playwright end-to-end suite with the existing seeded credentials, without exposing them.
- Preserve `source/` as read-only.
