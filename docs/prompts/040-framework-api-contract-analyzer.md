# Prompt 040: Framework API Contract Analyzer

Add reusable deterministic framework analyzers to the existing inventory, detection, fact, normalizer, and KG workflow. Support only ASP.NET route/contract facts, Razor links, and AngularJS API calls required by evidence; use no LLM. Regenerate the graph only through `create-knowledge-graph`, validate its integrity, measure mappings honestly, and mark existing application understanding stale when package hashes change. Do not generate modernization outputs.
