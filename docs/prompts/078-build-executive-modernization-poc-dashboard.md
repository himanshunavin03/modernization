# Prompt 078: Build Executive Modernization POC Presentation Dashboard

## User request

Build a completely separate, premium, static executive presentation dashboard under root-level `poc-dashboard/` for the Polaris Application Modernization POC. The presentation must tell the evidence-backed story `UNDERSTAND → MODEL → PLAN → MODERNIZE → VALIDATE`, emphasize deterministic analysis, Knowledge Graph traceability, requirements, Angular 22 architecture, live legacy/modern launch links, and Playwright validation.

The complete 60-section specification was supplied in the attached `pasted-text.txt` for this turn and is the authoritative request. Core boundaries include:

- Read existing approved artifacts and current Angular implementation only.
- Do not modify or regenerate source, product applications, modernization artifacts, or Playwright.
- Implement the presentation with offline HTML/CSS/minimal JavaScript and an optional standard-library Python presentation generator.
- Generate presentation-friendly Feature Specification, Technical Tasks, Application Understanding, and Playwright coverage pages plus audited dashboard data.
- Validate links, content provenance, responsive/accessibility behavior, and actual browser rendering at desktop and laptop viewports.
- Commit only `poc-dashboard/`, exactly once, as `Add modernization POC presentation dashboard` if every stated gate passes.
