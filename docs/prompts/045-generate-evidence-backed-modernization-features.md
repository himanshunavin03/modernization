# Prompt 045: Generate Evidence-Backed Modernization Features

Generate Features only from approved Application Understanding run `legacy-dashboard-complete-application-demo-v1-2026-09-01-054440-719694`, which is based on approved KG run `legacy-dashboard-complete-application-demo-v1-2026-09-01-034128` and readiness `APPLICATION_UNDERSTANDING_READY_WITH_LIMITATIONS`.

Preserve the established provider-free architecture: approved AU -> deterministic compact feature evidence packages -> active Codex reasoning -> structured feature candidates -> deterministic validation -> immutable feature catalog. Use zero external LLM API calls and never require provider credentials. Do not inspect the entire source repository for reasoning, modify `source/`, regenerate the KG/AU, load Neo4j, or infer unsupported business behavior.

Features must be concise business capabilities or modernization deliverables, not files, folders, controllers, endpoints, DTOs, classes, or methods. Each feature must identify its outcome, module/capabilities, supported personas, workflows, UI surfaces, domain concepts, rules, dependencies, API contracts and exact upstream API statuses, source/KG/AU evidence, confidence/provenance, modernization scope/priority/rationale, and limitations. Preserve `PROVEN`, `UNRESOLVED`, `DYNAMIC`, `EXTERNAL`, and `NO_BACKEND_ROUTE`; only upstream deterministic evidence can establish `PROVEN`. The modernization boundary is legacy frontend to Angular 22 with existing backend/API preserved.

Create stable feature evidence packages bound to both upstream run IDs and package hashes. Deterministically reject stale run IDs/hashes, nonexistent AU objects, unsupported source/KG/API references, unsupported proven mappings, duplicate IDs, empty outcomes, and untraceable features. Perform a functional quality review for duplication, technical/granular feature naming, missing capability/workflow coverage, and unsupported breadth. Keep the complete catalog separate from a small POC selection.

Select both approved Dashboard modernization surfaces. If evidence supports one business feature, keep Razor view `src/MyHealth.Web/Views/Dashboard/Index.cshtml` and legacy AngularJS `DashboardController` in that feature. Persist a new immutable run under `artifacts/features/runs/` and publish `artifacts/features/latest/` only after validation, including JSON catalog, Markdown summary, package manifest, validation report, POC selection, token usage, and upstream provenance.

Run focused feature package/hash/provenance/validation/API-status tests and the complete regression suite. Update `PROJECT_MEMORY.md` and `PROGRESS.md`; update `DECISIONS.md` only for a real architecture decision. Return `FEATURES_READY`, `FEATURES_READY_WITH_LIMITATIONS`, or `FEATURES_NOT_READY`. If ready, set `NEXT_ACTION=GENERATE_STORIES`, then stop without generating stories, acceptance criteria, architecture, Angular code, or source changes.
