# Prompt 037: Provider Configuration And Developer Experience

Add environment-driven provider selection and native developer interfaces over the shared CLI. Prefer configured Azure OpenAI or Bedrock; otherwise wait truthfully without fabricated understanding. Use Codex repository instructions and GitHub Copilot repository instructions/prompt files. Never duplicate KG logic, hard-code source paths, commit credentials, or claim unsupported slash-command discovery.
