# Prompt 031: Resume Complete Application Neo4j Validation

Read the complete-application failure disposition, final validation status, and recovery documents. Do not rerun parser repair unless the source fingerprint or extraction results change. Set Neo4j credentials only in the local environment, rerun the approved complete-application command with `--load-neo4j`, verify project-scoped counts match the canonical graph, and only then export and launch the local read-only viewer. Do not modify source or commit automatically.
