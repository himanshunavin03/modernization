# Prompt 081 — Add tokenized Knowledge Graph link

Add the running Understand Anything Knowledge Graph viewer to the POC presentation dashboard using its required tokenized direct URL, then verify the link opens successfully. Do not modify the read-only legacy project under `source/`.
