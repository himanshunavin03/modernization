# Prompt 053: Final Feature Specification Correction

Continue from `a4eb201197f385500bcea989ad5e561af79abedd` and preserve the approved deterministic KG through Feature Narrative/API Contract architecture and exact lineage. Keep five Features, 12 Stories, and 14 authoritative Acceptance Criteria unchanged. Do not analyze Figma, recommend architecture, generate Angular, alter backend/database/source, use an LLM, or regenerate the approved KG.

Re-evaluate Feature API completeness from approved Feature workflows, UI surfaces, frontend caller evidence, all 44 frontend API facts, all 58 backend endpoint facts, and source ranges. Classify each Feature-relevant interaction as primary business, supporting/shared, external, unresolved primary/supporting, or dynamic primary/supporting. Classification is Feature-relative: current tenant is supporting for Dashboard, Doctor, Patient, and Clinic, but may be primary for User Access/Tenant Context. Shared reuse is valid. A supporting API never constitutes primary coverage.

Where an associated caller proves a Feature-relevant frontend call and a same-domain backend endpoint exists without a deterministic mapping, retain the interaction as unresolved/dynamic and expose the endpoint only as `CANDIDATE_RELEVANT_BACKEND_ENDPOINT`. Never promote candidates by name similarity. Detect omitted API candidates, analyzer/enrichment gaps, and misleading supporting-only coverage. Generate `feature-api-coverage-matrix.json`, classification and contract validation, business language, Story, AC, customer comprehension, role readiness, and final quality artifacts.

Render customer-ready Feature Markdown with concise labeled summary content, business-first behavior, non-circular Stories, meaningful evidence-safe AC, and Section 5 split into Primary Business APIs, Supporting / Shared APIs, and Unresolved or Dynamic Integrations. Explain that confirmed backend contracts are preserved and candidates require confirmation. Keep technical evidence in the API section/appendix and preserve machine Feature JSON byte-for-byte.

Use evidence-based quality scoring across the requested business, Story, AC, API, technical completeness, readability, and role dimensions. Avoidable defects, invention, untraceable rendered fields, misleading API coverage, and scores below 9 block readiness. Read all five generated Markdown documents manually, run focused and full regression tests, verify immutable/latest parity and source/upstream integrity, update `PROJECT_MEMORY.md`, `PROGRESS.md`, and `DECISIONS.md`, then stop before design or architecture.
