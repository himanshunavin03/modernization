# Prompt 038: Local OpenAI Application Understanding

Add OpenAI as a local-demo implementation of the existing provider-neutral Phase-2 reasoning boundary without changing deterministic Knowledge Graph extraction. Select providers explicitly through `POLARIS_LLM_PROVIDER`; require `OPENAI_API_KEY` for OpenAI and never persist or print it. Reuse LangGraph, LangChain structured output, compact deterministic evidence packages, package-scoped KG evidence validation, stable per-package caching, and token usage records. Preserve Azure OpenAI and Bedrock implementations. If credentials are unavailable, create only a truthful `WAITING_FOR_PROVIDER_CONFIGURATION` result; do not fabricate business understanding, backend mappings, features, stories, architecture, or Angular output. Document the exact safe Codex invocation and test provider selection, malformed output rejection, unsupported-claim rejection, caching, token accounting, and zero-LLM KG creation.
