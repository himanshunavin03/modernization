# Prompt 082 — Show Polaris command workflow in dashboard

Update the executive POC dashboard to present the exact canonical Polaris custom command/plugin interface and explain, in customer-facing language, how the solution achieves the end-to-end modernization lifecycle from knowledge graph creation through requirements, architecture, technical planning, Angular modernization, traceability, and validation. Derive command names from the shared command registry; do not invent aliases or modify `source/`.
