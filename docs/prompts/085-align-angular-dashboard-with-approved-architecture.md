# Prompt 085 — Align Angular dashboard with approved architecture

Define the dashboard's Angular 22 architecture presentation from the approved artifact at `artifacts/architecture/latest/architecture.html`. Preserve the restored common Polaris Commands card view, use only architecture decisions supported by that artifact, retain traceability and runtime links, and do not modify `source/` or the approved architecture artifact.
