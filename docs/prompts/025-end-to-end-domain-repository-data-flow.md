# End-to-End Domain Repository Data Flow

Extend the isolated Dashboard context graph using only source/Roslyn evidence. Show Legacy Razor/AngularJS UI through API contract, controller/action, repository, EF/LINQ query, returned domain model, and `MyHealthContext`. Do not infer tables or SQL; raw SQL requires literal source evidence. Backend/data remain read-only and never Angular-generation candidates.
