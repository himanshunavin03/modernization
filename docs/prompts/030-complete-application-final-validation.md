# Prompt 030: Complete Application Final Validation

Validate the complete-application graph only after first-party native-crash fallbacks produce literal, source-backed facts and opaque records are restricted to proven generated, minified/tooling, or third-party dependencies. Run the complete graph with Roslyn and Neo4j loading, compare canonical and persisted counts, then export and launch the local read-only viewer only when all gates pass. Preserve source read-only boundaries and do not use LLM analysis.
