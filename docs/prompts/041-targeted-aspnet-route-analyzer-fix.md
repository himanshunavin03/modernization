# Prompt 041: Targeted ASP.NET Route Analyzer Fix

Repair the deterministic ASP.NET framework analyzer without changing legacy source or analyzer architecture. Replace fragile attribute capture with quote-aware, balanced attribute parsing; preserve class and method route templates, normalize only `[controller]` and `[action]` from declared controller/action evidence, and make API path fallback case-insensitive by path segment. Add generic regression fixtures and tests for route tokens, route-prefix composition, HTTP matching, ambiguous/dynamic/external calls, and API path case variants. Regenerate the complete Knowledge Graph only after tests pass, publish a new immutable run, perform a new forensic readiness gate, and keep Application Understanding stale until that gate determines the next action. Do not use an LLM or application-specific conditions.
