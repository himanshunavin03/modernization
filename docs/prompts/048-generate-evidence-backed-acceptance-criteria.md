# Prompt 048: Generate Evidence-Backed Acceptance Criteria

Generate the smallest complete, testable Acceptance Criteria set from approved Story run `legacy-dashboard-complete-application-demo-v1-2026-09-01-105453-078418`, preserving exact KG, Application Understanding, Feature, Business Feature, and Story lineage. Acceptance Criteria are downstream modernization-preservation contracts; do not modify approved Stories or upstream artifacts.

Use provider-free active Codex PO/BA/QA reasoning over deterministic one-Story evidence packages. Every criterion must have a stable ID, supported Given/When/Then context/action/outcome, business-readable condition, approved references, confidence, provenance, and evidence status. Preserve `PROVEN`, `UNRESOLVED`, `DYNAMIC`, `EXTERNAL`, and `NO_BACKEND_ROUTE` API truth. Keep unresolved clinic-summary, dynamic yearly-report, incomplete appointment, actor, and open-question limitations visible.

Do not invent personas, preconditions, functionality, rules, fields, validations, errors, messages, sorting, pagination, permissions, HTTP status codes, retries, NFRs, implementation tasks, architecture, design behavior, or tests. Unsupported edge cases remain potential considerations requiring requirement confirmation, not accepted criteria. Assumptions should remain zero.

Create compact AC packages, deterministic validation, immutable `artifacts/acceptance-criteria` runs, business-readable and machine-readable criteria, quality review, question backlog, requirements traceability, stakeholder review, QA handoff, and a POC modernization contract for the two approved Dashboard Stories. Do not split behavior merely because the legacy evidence spans Razor and AngularJS.

Record the future optional target-design boundary: after Acceptance Criteria, a supplied Figma design may inform design understanding/gap analysis before or with Target Architecture, but it must never redefine evidence-backed existing behavior. Do not analyze Figma or recommend architecture in this task.

If validation passes, return `ACCEPTANCE_CRITERIA_READY_WITH_LIMITATIONS` where inherited limitations remain and set `NEXT_ACTION=ANALYZE_OPTIONAL_TARGET_DESIGN_OR_RECOMMEND_TARGET_ARCHITECTURE`. Stop without executing that action.
