# Doctor Directory Management Stories

## Review Doctor Directory

As a user of the existing application, I want to review doctor directory, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to review doctor directory. This behavior also preserves the approved fixed ordering, the approved tenant context.

## View Doctor Details

As a user of the existing application, I want to view doctor details, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to view doctor details. This behavior also preserves the approved tenant context.

## Create Doctor

As a user of the existing application, I want to create doctor, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to create doctor. This behavior also preserves the approved tenant context.

## Update Doctor

As a user of the existing application, I want to update doctor, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to update doctor. This behavior also preserves the approved tenant context.

## Delete Doctor

As a user of the existing application, I want to delete doctor, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to delete doctor. This behavior also preserves the approved bulk operation behavior, the approved tenant context.

## Continue Through Doctor Results

As a user of the existing application, I want to continue through doctor results, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to continue through doctor results.

## Maintain Doctor Profile Media

As a user of the existing application, I want to maintain doctor profile media, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to maintain doctor profile media.

## Validate Doctor Input

As a user of the existing application, I want to validate doctor input, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to validate doctor input.

## Navigate from the Doctor Experience

As a user of the existing application, I want to navigate from the doctor experience, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to navigate from the doctor experience.

## Establish Current Tenant Context

As a user of the existing application, I want to establish current tenant context, so that I can use the approved doctor directory management behavior.

The existing application behavior requires users to establish current tenant context.
