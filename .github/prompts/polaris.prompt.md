---
description: Run a canonical Polaris modernization command
---

Interpret the user's canonical Polaris command using [the shared command guide](../../docs/agents/commands/polaris.md). Invoke the same `polaris` CLI/`CommandService` operation used by Codex and future adapters. Do not reproduce analysis, planning, generation, or validation logic in this prompt. Resolve Features through the generated Feature index, report ambiguity or prerequisites exactly, preserve artifact state, and never require an external model API key merely to invoke a command.
